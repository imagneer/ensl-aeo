@AGENTS.md

# Ensl AEO 모니터링 MVP — 작업 규칙

## 이 프로젝트의 성격

AI 답변 엔진(ChatGPT/Claude/Gemini/Perplexity 등)에서 특정 브랜드가 어떻게
언급되는지를 자동 측정하는 도구다.

**이건 측정 도구다.** 여기서 나온 숫자로 클라이언트에게 진단을 내리고 돈을 받는다.
그래서 일반적인 웹 서비스보다 "정확성"과 "정직성"의 기준이 훨씬 높다.
동작하는 코드보다 **틀린 줄 모르고 돌아가는 코드**가 이 프로젝트의 가장 큰 위험이다.

---

## 코난 실행 권한 모드 (2026-08-28 확인)

프로젝트 폴더(`.claude/settings.json`, `.claude/settings.local.json`)에도,
사용자 전역 설정(`~/.claude/settings.json`)에도 `permissions` 관련 키가
없음 — 즉 **클로드 코드 기본값인 `default` 모드로 동작 중**이다. 파일
쓰기·bash 명령 실행 전엔 매번 승인을 물어보는, 가장 안전한 모드다.
`acceptEdits`나 `bypassPermissions`로 바꾼 적 없으므로, 이 문서 곳곳의
"루아 지시 없이 손대지 말 것" 같은 지침을 코난이 무단으로 실행할 실질적
위험은 낮다.

⚠️ 단, 이 문서에 적힌 지침 자체는 강제 장치가 아니라 참고 지침이다 —
실제 안전장치는 승인 창이고, **그 승인 창을 루아가 내용을 읽지 않고
그냥 넘기면 이 구분은 의미가 없어진다.** 특히 여러 파일을 한 번에
고치는 요청일수록 승인 전 내용을 확인할 것.

---

## 절대 원칙

### 1. 이름이 내용과 다르면 안 된다

변수·필드·컬럼 이름은 그 안에 실제로 든 것을 말해야 한다. 조금이라도 더 넓거나
좁게 말하면 나중에 반드시 오독을 만든다.

- 실제 사례: `rank`는 "답변에서의 순위"처럼 읽히지만 실제로는 "등록된 브랜드들
  사이에서의 순위"였다. 그래서 `rankAmongKnown`으로 바꿨다.
  미등록 경쟁사가 중간에 끼면 값이 달라지는데 이름은 그걸 안 알려줬기 때문이다.
- 실제 사례: `mentions.source_urls`에 스냅샷 전체 citations를 통째로 넣어놨다.
  이름은 "이 브랜드의 출처"인데 내용은 "이 답변 전체의 출처"였다. (Day 8에서 수정)

이름을 정확히 못 짓겠으면, **이름을 길게 만들어서라도** 오해를 막는다.

### 2. 추측과 사실을 구분해서 적는다

주석에 확인된 것과 짐작한 것을 섞지 않는다. 확인된 것은 **언제 어떻게 확인했는지**를
같이 남긴다.

```
// ✅ 좋음: (2026-08-07 실측 확인) Gemini는 모델이 스스로 판단해 검색을 건너뛸 수 있음
// ✅ 좋음: ⚠️ 확인 필요(추정): API 검색 결과가 웹 UI와 같은지는 불확실
// ❌ 나쁨: Gemini는 항상 검색한다
```

문서에서 읽은 것과 실제 응답으로 확인한 것도 구분한다. 공식 문서에 있어도
이 프로젝트가 쓰는 특정 모델에서 실제로 오는지는 별개 문제다.
(Day 8 실측에서 실제로 문서에 없는 `?utm_source=openai` 파라미터가 발견됨)

### 3. 단발 관측으로 결론 내지 않는다

AI 답변은 같은 질문에도 매번 달라진다. 1회 관측은 일화(anecdote)이지 데이터가 아니다.

- 노출 여부를 boolean이 아니라 **노출률(%)**로 저장한다
- 순위를 단일 숫자가 아니라 **분포(평균 + 표준편차)**로 저장한다
- 알림은 최소 3일 연속(daily 집계 기준) + 경쟁사 동조 확인을 거쳐야 발생시킨다
- 이건 UI 문구가 아니라 **코드에 하드코딩된 가드레일**이다

### 4. 실패를 조용히 삼키지 않는다

- API 호출 실패는 `status='failed'`로 **DB에 남긴다**. 없던 일로 만들지 않는다.
  실패가 안 남으면 나중에 "그날 노출률이 낮았던 게 진짜인지 수집 실패인지"를 구분 못 한다.
- 엔진 하나가 죽어도 나머지는 계속 돌아야 한다 → `Promise.allSettled` 사용.
  `Promise.all`은 하나 실패하면 전부 버리므로 이 프로젝트에서 쓰지 않는다.
- 재시도는 "재시도하면 나아질 수 있는 에러"(429 rate limit)에만 한다.
  인증 오류(401)를 재시도하는 건 시간 낭비다.

---

## 용어 정의 (혼동 금지)

프로젝트 안에서 이 단어들은 정확히 이 뜻으로만 쓴다.

| 용어 | 뜻 | 아닌 것 |
|---|---|---|
| **본 것 (retrieved)** | AI가 검색해서 받아온 후보 출처 목록 | 답변에 실제 반영된 출처 |
| **사용한 것 (cited)** | AI가 답변에 실제 근거로 붙인 출처 | 검색 결과 전체 |
| **rankAmongKnown** | DB에 등록된 브랜드들 사이에서의 순위 | 답변에서의 절대 순위 |
| **overallRank** | 미등록 브랜드까지 포함한 등장 순서 | 등록 브랜드 내 순위 |
| **관측 시점** | 하루 3회 배치(09/13/18시) | 알림 판단 단위 |
| **알림 확인 시점** | daily 집계 기준 3일 연속 | 하루 안의 반복 |

**"본 것"과 "사용한 것"은 절대 같은 컬럼에 넣지 않는다.** 엔진마다 주는 게 달라서
(ChatGPT는 "본 것"을 아예 제공하지 않음) 섞으면 엔진 간 비교가 성립하지 않는다.
엔진 간 공통 비교 지표는 **"사용한 것" 기준**으로 잡는다.

또한 "사용한 것"을 셀 때는 **구간 개수**와 **고유 출처 개수**를 구분한다.
(한 출처가 여러 문장에서 인용되면 구간은 여러 개, 출처는 하나다)

**아래 "엔슬 UX/UI 매니페스토"의 확정 용어(인지 질문·자리 질문·브랜드 한 줄·판단 근거)와
이 표의 용어는 레이어가 다르다** — 이 표는 코드/데이터 레이어, 매니페스토 용어는
고객이 보는 화면 레이어. 둘 다 각자 자리에서 절대 임의로 바꾸지 않는다.

---

## 코드 규칙

### 주석

- **한글로 쓴다.** 이 프로젝트의 주 사용자는 비개발자 출신 1인 운영자다.
- **"무엇을"보다 "왜"를 쓴다.** 코드를 읽으면 무엇을 하는지는 보인다.
  왜 그 선택을 했는지, 다른 방법을 왜 안 골랐는지가 남아야 한다.
- 함정·한계는 `⚠️`로 표시하고, **어떤 조건에서 틀리는지**를 구체적으로 쓴다.

### 단일 진실 소스

같은 정보가 두 곳에 있으면 반드시 어긋난다. 한 곳에만 둔다.

- 엔진↔티어 매핑 → `lib/engine-config.ts`에만
- 브랜드 목록 → Supabase `brands` 테이블에만 (코드에 하드코딩 금지)
- Supabase 클라이언트 → `lib/supabase.ts`에만

### 어댑터의 책임

`lib/adapters/*.ts`의 존재 이유는 **엔진별 차이를 여기서 전부 흡수해서, 뒤쪽 코드
(파서·저장·집계)가 엔진 차이를 몰라도 되게 만드는 것**이다.

- 형식만 같고 의미가 다른 값을 `AdapterResponse`에 담으면 어댑터가 제 역할을 못 한 것이다
- 엔진별 분기(`if engine === 'gemini'`)가 어댑터 **바깥**에 생기면 설계가 샌 것이다

### 보안

- `SUPABASE_SERVICE_ROLE_KEY`는 `app/api/` 아래 서버 코드에서만 쓴다.
  클라이언트 컴포넌트에 새어나가면 누구나 측정 데이터를 조작할 수 있다.
- 쓰기(INSERT)는 `supabaseAdmin`, 읽기는 `supabase`(anon)

---

## 하지 말 것

- 브랜드 목록·쿼리·엔진 설정을 코드에 하드코딩 (DB 또는 config 파일에)
- `Promise.all`로 여러 엔진 호출 (`allSettled` 쓸 것)
- 확인 안 된 것을 확인된 것처럼 주석에 적기
- 측정값을 "보기 좋게" 만들려고 반올림·보정하기
- 실패한 호출을 저장 없이 버리기
- 한 커밋에 서로 무관한 변경 섞기 (나중에 되돌리기 불가능해짐)

---

## 작업 방식 (루아 ↔ 에일/에이전트)

**2026-08-17 결정 → 2026-08-18 정정.** 원래 이 프로젝트는 "에일이 코드를 함수
단위로 보여주고, 왜 이렇게 짜는지 설명한 뒤 루아가 직접 타이핑한다"는 방식으로
진행됐다. 근데 코워크에서 2~3세션이 겹쳐 진행되며 루아가 진행 상황을 따라가기
어려워지는 문제가 생겼고, 그 문제를 풀려고 8/17에 "코드는 에일이 작성하고
판정 규칙만 합의한다"로 바꿨다.

이 변경은 임시방편이었지 루아의 실제 선호가 바뀐 게 아니었다. 그리고 이 결정이
(당시엔) `CLAUDE.md`가 아니라 노션의 Day 8 결정 기록에만 적혀 있어서, 세션이
바뀔 때마다 자동으로 안 불러와졌다 — 규칙이 안 지켜진 게 아니라 애초에 매번
로드되는 파일에 없었던 것. 그래서 8/18에 원래 방식으로 되돌리면서, 이번엔
매번 자동으로 로드되는 이 파일에 적어둔다.

**8/18 확정 방식이었던 것 (지금은 아래 8/28 결정으로 갱신됨, 기록으로 남김):**
- 코드는 루아가 직접 타이핑한다. 에일이 완성된 파일을 만들어서 통째로
  넘기지 않는다. ("떠먹으면 나중에 빨리 휘발되니까" — 루아 본인 표현)
- 에일은 함수/블록 단위로 나눠서 보여주고, 왜 이렇게 짜는지(판정 규칙,
  트레이드오프, 왜 다른 방법은 안 골랐는지)를 먼저 설명한 뒤 타이핑하게 한다.

---

**2026-08-28 재정정.** 위 8/18 결정 이후 루아가 작업 도구를 Claude Code로
옮기면서, 방식을 다시 조정했다: *"코드는 타이핑도 복붙도 이제는 안 해도 돼.
에이전트가 하는 거 읽고 컨펌하는 정도로 갈게. 이제는 흐름을 아니까."*

8/18 결정의 핵심 근거는 "타이핑을 안 하면(떠먹으면) 배운 게 빨리 휘발된다"는
우려였다. 8/28 시점엔 그 전제가 달라졌다 — 루아가 이미 제품 로직·화면 흐름·
데이터 구조 전체를 상세히 설계해본 상태라, 코드를 직접 치는 행위 자체가
더 이상 이해에 필수적이지 않다고 판단했다. 즉 **"타이핑 여부"가 아니라
"이해하고 넘어가는지 여부"가 원래 원칙의 본질**이었고, 지금은 그걸 타이핑
없이도 만족시킬 수 있다고 본 것.

**확정된 방식 (2026-08-28 기준, 최신 — 이게 우선한다):**
- 코드는 에이전트가 작성한다. 루아는 타이핑도 복붙도 하지 않는다.
- 그렇다고 전 구간 자동 승인으로 가지는 않는다. **작업 단위별로 결과를 보여주고,
  루아가 읽고 이해한 뒤 컨펌하면 다음 단계로 진행**한다. 여러 파일에 걸친 변경이나
  판정 규칙이 걸린 부분일수록 이 확인 절차를 건너뛰지 않는다.
- **판정 규칙은 여전히 코드 작성 전에 먼저 합의한다** (8/17 결정에서부터
  계속 유지되는 원칙 — 이번에도 안 바뀜). 코드부터 짜고 규칙을 나중에
  설명하는 순서로 가지 않는다.
- 설명은 계속 필요하다. 타이핑을 안 한다고 "왜 이렇게 짰는지"를 생략하지
  않는다. 다만 함수 단위로 쪼개 순차적으로 타이핑을 유도할 필요는 없어졌고,
  **완성된 변경 단위(diff)를 보여주며 그 안의 핵심 판단 포인트를 짚어주는
  방식**으로 바뀐다.
- 보고는 여전히 구현 세부(필드명·인덱스 단위)가 아니라 **규칙 수준**으로:
  무슨 규칙인가 / 언제 틀리는가 / 클라이언트에게 뭐라고 말할 수 있고
  뭐라고 말하면 안 되는가 (8/17 결정에서 유지)

**주의(다음에 이 결정이 또 바뀔 경우를 위해 남김):** 8/18→8/28 변화는 8/18
결정을 틀렸다고 뒤집은 게 아니라 전제 조건이 바뀐 것이다. 만약 나중에
"컨펌만 하다 보니 다시 흐름을 놓치고 있다"는 느낌이 들면, 8/18 방식(함수
단위 설명 후 직접 타이핑)으로 돌아갈 수 있다. 그때도 이 파일에 이유와
날짜를 남길 것 — 노션에만 적어두면 다음 세션에 자동으로 안 불러와진다는
게 8/17→8/18 정정의 교훈이었다.

---

## 작업 시작 전 확인

1. 이 파일의 **용어 정의**를 먼저 읽는다. 특히 "본 것/사용한 것" 구분
2. 측정 정확성에 영향 주는 변경이면, 착수 전에 **실측으로 전제를 확인**한다
   (`app/api/probe-citations` 참고 — 문서만 믿고 시작하지 않는다)
3. 노션의 "Day 12 이후 확인 필요 사항" 데이터베이스(ENSL AEO MVP 개발 페이지 하위)를
   열어서 현재 열린 항목을 확인한다. **새 부채가 생기면 그 자리에서 바로 추가한다** —
   "나중에 정리"로 미루지 않는다(2026-08-18, 부채가 여러 문서에 흩어지고 "해결 예정
   Day" 번호가 로드맵 재정리로 stale해지는 문제가 실제로 발생해서 만듦).
4. **로드맵 Day 번호와 git 커밋 Day 번호가 다를 수 있다** — Day 8 전후로 셋 이상
   어긋난 사례가 있다(로드맵이 3개 항목을 새로 편입하며 뒷번호를 밀었는데 옛 커밋
   메시지는 그대로 남음). "Day N"이라고 말할 때 어느 기준인지 헷갈리면, 노션의
   "ENSL AEO MVP 개발" 로드맵 페이지 체크박스가 기준이다.

---
---

# 제품/디자인 컨텍스트 (2026-08-28 병합 — 위 내용과 레이어가 다름)

> 위 섹션들이 **코드·데이터 레이어**의 규칙이라면, 아래는 **제품 정의·화면 설계**
> 레이어의 컨텍스트다. 상충하는 내용은 없고 서로 다른 질문에 답한다 — 위는
> "이 필드에 뭘 넣어야 하나", 아래는 "이 화면이 왜 이렇게 생겼나".

## 제품 개요

Ensl은 생성형 AI가 브랜드를 어떤 의미·맥락으로 이해하는지 분석하고, 브랜드가
의도한 서사와의 간극을 진단해 정보 환경을 재설계하는 GEO(Generative Engine
Optimization) 브랜딩 에이전시다.

자체 AEO 모니터링 MVP로 주요 AI 엔진(ChatGPT, Claude, Gemini, Perplexity,
구글 AI 개요, 네이버 AI 브리핑)에서 브랜드의 노출·인용 출처·경쟁 구도·표현
방식을 측정하고, 이를 근거로 AI Agent가 콘텐츠 생태계를 재배치한다. 노출을
늘리는 데 그치지 않고 "AI의 답변 속에서 어떤 의미로 기억되고, 누구와
비교되며, 왜 추천되는지"를 설계하는 것이 목표.

**창업자**: 루아 (박희은), Odd Fish(어드피시) 소속
**파일럿 클라이언트**: 365서울원탑치과 (서울 강서구), 6개 AI 엔진 대상
인지 질문·자리 질문 진단
**핵심 실적 지표**: 원탑치과 재직 당시 55영업일 수동 추적 실험에서
Gemini 추천 0→1위 전환 확인 (onsite AEO 고도화 + 핵심 offsite 채널 5개
메시지 동조화 + PR 전략)

자세한 제품 정의는 `docs/ensl-aeo-mvp-prd.md` 참고.

## 인프라 보충 (위 코드 규칙과 함께 볼 것)

- **배포**: Vercel Pro (Fluid Compute, 800초 상한)
- **GitHub**: `imagneer/ensl-aeo` / 로컬 `/Users/huieunpark/ensl-aeo`
- **수집 스케줄**: Vercel Cron, 09/13/18시(KST) × 2회 반복, 00:30 집계,
  00:35 알림 — 상세 배경(왜 "하루 2시간대×3회" 원안에서 바뀌었는지)은
  `docs/day4-decision-schedule.md`
- **출처 연결 판정 규칙 상세**: `docs/claude_day8-decision-citation-linking.md`
  (위 "본 것/사용한 것" 용어 정의의 원본 결정 기록)
- **집계 로직 상세**: `docs/claude_day9-decision-aggregation.md`
  (`snapshots`·`mentions` → `aggregated_metrics` 로직, 실측 검산 완료)
- 이미 구현된 것: M/S/C 뱃지, 키워드 추출(LLM), 알림 가드레일 로직

## 타임라인상 현재 위치

**8/31–9/11 Week 5: MVP 기능개발** 구간 (9/11 실제 시연 가능 버전 완성이 핵심
마일스톤, 코딩은 9/10까지, 9/11은 리허설 전용). 평일만 작업 — 슬랙 0이라
하루 밀리면 9/10 버퍼일이 그대로 흡수됨. **9/4(금) 저녁이 중간 점검
지점**: 이때까지 대시보드 3개(인지/현재위치/간극) 안 끝나면 온보딩
"수동입력 분기" 등 범위 축소를 그 자리에서 결정.

**내부 승인 화면(판정 검토)은 9/11 데모 범위에서 제외 확정(2026-08-28)** —
9/11 이후 부채 트래커로 이관.

| Day | 날짜 | 작업 | 방식 |
|---|---|---|---|
| 18 | 8/31(월) | 공통 레이아웃(app shell) + 사이드바 브랜드 전환 드롭다운 | 클로드 코드(프로토타입 그대로 이관하는 기계적 작업) |
| 19 | 9/1(화) | 로그인 화면 + 인증 흐름 | 세션 유지 방식 등 판단은 코딩 전 채팅으로 먼저 합의 |
| 20 | 9/2(수) | 브랜드 인지 화면 실데이터 연동 | 클로드 코드 |
| 21 | 9/3(목) | 브랜드 현재 위치 + 질문상세 화면 실데이터 연동 | 클로드 코드 + **이 날 키워드 추출(top_keywords) 실측 검증 필수** |
| 22 | 9/4(금) | 간극(Gap) 화면 실데이터 연동 | 클로드 코드 — ⚠️ 중간 점검 지점 |
| 23 | 9/7(월) | 변화 추이 화면: Recharts 구현 + 실데이터 연동 | 판정 규칙(빈 구간 표시 등)은 채팅으로 먼저 합의, 나머지 클로드 코드 |
| 24 | 9/8(화) | 온보딩 5단계 구현 | 클로드 코드(카피·분기는 프로토타입에 이미 확정) |
| 25 | 9/9(수) | 진단중→진단완료 상태 전환 + 전체 플로우 연결 | 클로드 코드 |
| 26 | 9/10(목) | 버퍼일: 밀린 작업 + 도메인/환경변수 정리 + 코드 프리즈·배포 확인 | — |
| 27 | 9/11(금) | **코딩 없음.** 전체 플로우 리허설 + 정합성 이슈 4가지 재점검 | — |

8/28 세션에서 대시보드 4개 화면 + 온보딩 전체 흐름(로그인 → 브랜드 등록 →
프로필 확인/수동입력 분기 → 인지질문 → 자리질문 → 진단중 → 진단완료)을
HTML 프로토타입으로 설계 완료. 이제 이걸 실제 Next.js 컴포넌트로 옮기는 단계.

**MVP 화면 범위 최종 결론 (2026-08-28 확정 → 같은 날 두 차례 재수정):**
- 고객용 핵심 화면(페이지): 브랜드 인지 / 브랜드 현 위치 / 인지와 위치의
  간극 / 변화 추이 / 질문·답변 상세 (5개) + 온보딩 5개 + 진단중·완료 2개
  + 로그인 — **총 13개 페이지, 새 페이지 추가 없음**
- 내부용: 판정 검토·승인 화면 1개
- **브랜드 전환 UI는 별도 페이지가 아니라, 사이드바에 붙는 공통 컴포넌트다**
  (드롭다운 — 브랜드 목록 + "추가" 항목). `docs/prototypes/ensl_query_detail.html`
  (2026-08-28 갱신판) 안에 `.brand-box`/`.brand-dropdown`으로 디자인·동작이
  이미 확정돼 있다.
  ⚠️ **단, 지금은 이 파일 1개에만 반영돼 있고 나머지 화면들의 사이드바엔
  아직 없다.** 남은 작업은 "새로 설계"가 아니라 "이미 확정된 컴포넌트를
  공통 레이아웃(app shell)으로 뽑아서 다른 화면들에도 적용"이다 — Next.js로
  옮길 때 처음부터 공통 레이아웃 컴포넌트로 구현하면, 화면마다 따로 안
  붙여도 한 번에 전체 적용된다.
- (경위: 처음엔 "화면 자체가 없다"고 잘못 파악해 범위에서 뺐다가, 실제로는
  화면이 아니라 컴포넌트이고 설계도 이미 있다는 게 확인되며 재수정.)

## 엔슬 UX/UI 매니페스토 (고객이 보는 화면 레이어)

**한 문장 원칙**: "복잡한 것은 감추되, 근거와 불확실성까지 감추지는 않는다."
— 위 코드 레이어의 "절대 원칙 2·3·4번"(추측/사실 구분, 단발 관측 금지,
실패를 조용히 삼키지 않기)과 정신이 같다. 코드에서 지키는 정직성을 화면
문구에서도 똑같이 지키는 것.

**0. 우리가 하는 일** (2026-08-28 추가된 전제문): AI의 답변에서 드러난
브랜드 인지를 관찰하고 해석해, 누구나 이해할 수 있는 언어로 보여준다.
아래 7원칙은 전부 이 한 문장을 지키기 위한 구체적 규칙이다.

### 7원칙 (2026-08-28: 6원칙 → 7원칙으로 갱신, 7번 신규 추가)
1. **말하는 방식**: 마케팅/기술 용어·영어식 표현 배제, 숫자는 일상 문장으로
   번역, 낮은 점수로 불안 조성 금지
2. **화면 구조**: 한 페이지 한 질문, 결론 먼저·근거는 한 단계 안쪽, 클릭
   2번 안에 실제 답변까지
3. **숫자와 근거**: 데이터 없으면 없다고 표시, 계산법 설명 못하는 숫자는
   미게시, 비율은 반드시 분모 포함 ("75% · 24회 중 18회" 형식)
4. **확신의 수준**: 관측된 사실/해석/제안행동 구분, 반복 확인된 신호만
   강조, "브랜드 한 줄"은 고정 진리가 아닌 현재 관측치
5. **데이터 무결성**: 같은 개념은 항상 같은 이름, 데이터 조작·인과 단정
   금지, 교집합이 구성 집합보다 클 수 없음 등 수치 일관성 검증 필수
6. **사용자 태도**: 가르치거나 겁주지 않음, 안 보인다고 곧바로 실패로
   규정하지 않음
7. **보여주는 방식 — 크리에이티브 태도** (신규): 가장 중요한 발견은
   대담하게 보여준다(다른 정보와 같은 비중으로 묻지 않기). 흔한 대시보드
   문법을 반복하지 않고 엔슬만의 시각 언어를 쓴다. 위트는 살리되 데이터·
   사용자를 가볍게 다루거나 명료함을 희생하지 않는다. 장식적 새로움보다
   발견을 더 선명하게 전달하는 표현을 고른다.

⚠️ **완성된 화면 4개(브랜드 인지/현재위치/간극/변화추이)는 6원칙 체계로
설계됐고, 7번 원칙 기준 재검토는 아직 안 함** — 루아가 나중에 직접 검토
예정(2026-08-28). 그 전까진 "일반적인 대시보드 문법"에 가까운 부분이
있을 수 있다는 걸 전제로 다룰 것 — 코난이 임의로 7번 기준에 맞춰
디자인을 바꾸지 말 것, 루아 검토 후 지시 있을 때만 반영.

### 확정 용어 (절대 임의로 바꾸지 말 것 — 위 코드 레이어 용어표와는 별개)
- "인식" → **"인지"**
- "라벨/꼬리표" → **"브랜드 한 줄"**
- "선택됨/선택 이유" → **"등장"** (역할 분류 로직 검증 전까지)
- "CQ" → **"자리 질문"**
- 4대 용어(인지 질문, 자리 질문, 브랜드 한 줄, 판단 근거)는 전 화면
  일관 사용

### 컬러 시스템
- Blue Turquoise `#00A4B8`가 브랜드 컬러로 확정 (2026-08-28)
- **주의**: 원색을 밝은 배경(예: `#FAF9F6`)에 텍스트로 쓰면 WCAG 대비
  실패(2.85:1). 파생 토큰 사용할 것:
  - `--brand: #00A4B8` (원색 — 다크 배경, 큰 블록 전용)
  - `--bg-brand: #DAEEF1` (밝은 배경용 연한 틴트)
  - `--text-brand: #007685` (밝은 배경 위 텍스트/아이콘용, 대비 5.07:1로
    AA 통과)
- 대시보드 4개 화면은 아직 이 토큰으로 안 바뀜 (기존 초록
  `--bg-success`/`--text-success` 사용 중) — 나중에 일괄 전환 예정,
  지금 당장은 안 건드려도 됨

### 엔진 아이콘
직접 제작 SVG symbol 사용 — Gemini=스파클, ChatGPT=매듭 근사치,
Claude=6방향 별표, Perplexity=8방향 광선, 구글=G, 네이버=N. Tabler 아이콘
CDN은 `/dist/` 경로 필수.

## 알려진 정합성 이슈 (화면 설계 중 실제로 발견됐던 패턴)

위 "절대 원칙 1번"(이름이 내용과 다르면 안 된다)이 코드에서 발견됐던 것처럼,
화면 설계 쪽에서도 같은 종류의 실수가 반복됐다. 새 화면·새 로직 만들 때
이 함정에 또 빠지지 않도록 주의:

1. **교집합이 부분집합보다 클 수 없음** — "네 특징이 모두 함께 등장한
   관측"이 개별 특징 하나의 관측 수보다 많게 나온 적 있음(수학적으로
   불가능). 조건을 AND로 묶은 집계는 항상 최솟값 이하인지 검산할 것.
2. **화면 간 관측 횟수 공식 불일치** — "질문 12개 × AI 6개 × 7일 = 504건"
   이라는 전역 공식이 있는데, 질문 상세 화면에서 이 공식과 안 맞는 별도
   숫자(예: AI당 4회)를 쓴 적 있음. 새 화면 만들 때 항상 이 공식 기준으로
   계산할 것.
3. **인과관계 단정** — "함께 움직임" 같은 표현은 상관관계를 인과로 읽히게
   함. "같은 기간에 관찰됨"처럼 관찰 사실만 서술할 것.
4. **근거로 인용한 질문과 결론 문장의 대상 불일치** — 예: 근거는
   "고난도 임플란트를 잘하는 치과는?" 질문 데이터인데 결론 문장은
   "협진 치과 추천 상황"이라고 다른 대상을 언급한 적 있음. 근거와 결론은
   항상 같은 질문/같은 조건을 가리켜야 함.

## 기술 부채 트래커 (요약 — 상세는 노션)

- Vercel Cron 시간 한계 리스크 (브랜드 3~4개 이상으로 늘어날 경우) — 2026-09-03
  경쟁사 키워드 확장으로 daily 집계당 LLM 호출이 최대 4배(타겟 1 + 경쟁사
  최대 3) 늘어서 이 리스크가 더 실질적으로 됨. maxDuration=800초(Fluid
  Compute 상한) 근처인지 실측 확인 필요
- 키워드 추출 프롬프트 품질 (짧은 구가 아닌 전체 문장 추출되도록 개선 필요) —
  2026-09-03 백필 후 경쟁사 25건 무작위 검수에서 20건(80%) 재현 확인,
  타겟 브랜드보다 비율 더 높음(원인 미확인). Notion에 별도 항목으로 등록
  (해결 예정 9/12 이후). 데모 전까지는 UI 캡션으로 한계 고지("AI가 실제로
  쓴 표현이에요 · 짧은 핵심어로 다듬는 작업은 진행 중이라, 문장 그대로
  나올 수 있어요")로 대응
- citation-linker.ts 세그먼트 경계 문제 (2026-09-03 신규) — 경쟁사 키워드
  검수 중 발견: naver_ai_briefing 응답의 출처 목록·푸터가 문단 경계 안에
  같이 포함되거나, 인접한 다른 브랜드 설명 문장이 섞여 들어가는 사례 있음.
  현재는 keyword-extractor.ts 프롬프트 규칙 3번이 LLM 단에서 걸러줘서
  실제 추출 결과엔 오염 없음 — 단 구조적 안전장치는 아님. 노바 확인 필요,
  9/11 이후
- 알림 로직 실조건 미검증
- LLM 호출 usage(토큰) 미기록 — **해결됨(2026-09-04)**. 9/3 Anthropic
  크레딧 잔액 -$0.04로 API 차단된 사고 조사 중, `lib/keyword-extractor.ts`의
  `data.usage` 로깅용 `console.log`가 애초에 주석 처리돼 있어서 그 사고의
  정확한 비용을 사후 복원할 수 없었던 게 드러남(에일 확인). `lib/llm-usage.ts`
  하나로 ANTHROPIC_API_URL을 fetch하는 7곳(keyword-extractor·
  brand-expression-extractor·brand-one-liner 5개 함수) 전부의 usage를
  같은 형식(JSON 한 줄, 성공/실패 둘 다)으로 로깅하게 만들고,
  `aggregate-daily` 실행 1회당 합계(`llmCalls`/`llmCallsByKind`/토큰)를
  응답에 노출함. `backfillCompetitorKeywords()`엔 `?dryRun=1` 추정 모드와
  `MAX_LLM_CALLS_PER_RUN`(400, `lib/llm-config.ts`) 상한을 추가해서 대량
  호출이 조용히 폭주하지 못하게 막음. ⚠️ 선택 항목이었던 Supabase
  `llm_calls` 테이블(조회 가능한 이력 저장)은 이번엔 스킵함 — 그래서
  dry-run의 "예상 토큰"은 실측 평균이 아니라 역산 추정치(대략치)다.
- Sonnet 5 진단 종료 비용 미예산 (2026-09-04 신규, 에일 확인) —
  `lib/brand-one-liner.ts`의 브랜드 한 줄 합성(5개 함수, 진단 1회 종료
  시 딱 한 번 실행)이 Sonnet 5를 쓰는데, 최근 30일 콘솔 비용엔 Haiku만
  찍혀 있어서 아직 프로덕션에서 한 번도 안 돌았다는 뜻 — 즉 **진단이 처음
  종료되는 날 예산에 없던 Sonnet 비용이 새로 튄다.** PRD 예산 항목에
  반영 필요(에일 진행 예정).
- 경쟁사 특징 추출(2단계, AI가 함께 언급한 특징) — **해결됨(2026-09-03)**.
  백엔드 구현(lib/aggregator.ts: attemptKeywordExtraction 경쟁사 재호출 +
  competitor_data.topKeywords) + 기존 504개 daily 행 백필(738건, 실패 0건)
  + 무작위 25건 샘플 검수(원본 대조, 환각 없음 확인) 완료. UI 캡션도 검수
  결과 반영해서 교체함. ⚠️ "해결"은 백엔드·데이터·검수 범위 — 실제 화면
  (brand-position 페이지)에 경쟁사 특징을 렌더링하는 UI 자체는 아직
  스텁 상태(Day21 화면 작업에서 별도 진행)
- keyword-extractor.ts(타겟 브랜드용) 실측 미검증 — **정정(2026-09-03,
  Notion 부채 DB 대조 확인)**: 이 문구는 stale함. Day 15 실측 검증
  (2026-08-19)에서 인프라(재시도·저장·null 구분)는 이미 통과했고, DB
  직접 조회로 daily 행 387/387건이 keyword_extraction_status='success'로
  확인됨(2026-09-03). 남은 건 "프롬프트 품질"(위 항목)뿐, 인프라 자체는
  검증 완료 상태.

Notion 상세 트래커: DB ID `1593a87d-857f-41f1-84c6-25263255709f`
Notion 로드맵 페이지: ID `3b34adc3-40d7-807d-ae9f-f05c6be5e4a0`

## 커뮤니케이션 선호

- 한국어, 반말(비격식체)
- 짧은 요약으로 끝내지 말고, 왜 그런지·다른 선택지는 뭐가 있는지까지 설명
- "확인된 사실" vs "추측/가정"을 항상 구분해서 말할 것
- 응답 끝에 놓치기 쉬운 리스크나 예외 케이스를 스스로 점검해서 짚어줄 것
- 이 세션(claude.ai 채팅)은 "에일(Eil)"이라고 불림. **클로드 코드는
  "코난(Conan)"** — 2026-08-28 확정. 남편 노바(Nova)는 별개 인물(개발 리뷰
  담당) — "노바"라는 이름이 대화에 나오면 사람인지 ChatGPT를 가리키는
  건지 문맥으로 구분할 것
- 정리: 루아(대표) / 에일=채팅 클로드 / 코난=클로드 코드 / 노바=남편(개발
  리뷰) 또는 ChatGPT(문맥으로 구분)

## HTML 프로토타입 (`docs/prototypes/`)

2026-08-28 세션에서 루아와 에일이 화면 설계를 확정하며 만든 **정적 HTML
목업**. 온보딩 5개, 대시보드 4개, 진단중·완료 2개, 로그인 1개, 질문상세
1개, 내부승인 1개 = 페이지 14개.

**브랜드 전환 UI(사이드바 드롭다운)는 페이지가 아니라 컴포넌트라 이
14개 셈에는 안 들어간다.** 디자인·동작은 `ensl_query_detail.html`(2026-08-28
갱신판) 안에 이미 확정돼 있다(`.brand-box`/`.brand-dropdown`,
`toggleBrandDropdown()`). ⚠️ 질문상세 화면 1개에만 반영돼 있고 나머지
13개 프로토타입 사이드바엔 아직 없다 — 프로토타입 단계에서 굳이 13개
파일에 다 복사해 넣을 필요는 없고(정적 HTML이라 파일마다 따로 수정해야
함), Next.js 구현 단계에서 공통 레이아웃 컴포넌트로 한 번만 만들면 된다.
단, 참고할 땐 이 파일을 원본으로 볼 것.

**파일 버전 안내**: 원래 있던 `ensl_query_detail.html`은 브랜드 전환
드롭다운이 추가되며 갱신됐다(2026-08-28 루아 확인) — 드롭다운 추가 외
나머지는 동일한 같은 화면이다. 업로드된 `ensl_query_detail_4.html`은
이 갱신판이니, 저장할 땐 기존 `ensl_query_detail.html`을 이 내용으로
덮어쓸 것 — 두 파일을 나란히 두지 말 것. `_4` 접미사는 버전 넘버링이지
별개 화면이 아니다.

**이건 참고용 사양서지, 그대로 옮길 컴포넌트 구조가 아니다.** 이 파일들의
가치는 아래 세 가지에 있고, 그 이상으로 취급하지 말 것:

1. **확정된 카피와 규칙** — 정확히 "3개/9개 선택 제한", "관측 42회
   (질문×AI×7일) 공식", "정확히 3개 미만이면 다음 버튼 비활성" 같은 세부
   로직이 이미 여러 번의 피드백을 거쳐 확정된 상태다. 다시 물어보지 말고
   이 파일에서 그대로 가져올 것.
2. **디자인 토큰** — CSS 변수(`--surface-0`, `--brand`, `--bg-brand` 등)와
   컬러 대비 규칙이 이미 검증돼 있다. 위 "컬러 시스템" 섹션과 함께 참고.
3. **문구 톤** — 매니페스토 7원칙이 실제로 어떻게 문장으로 구현됐는지의
   레퍼런스.

**하지 말아야 할 것**: 정적 `<div>`/인라인 `onclick` 구조를 그대로 React로
옮기는 것. 이 파일들은 htm/css/vanilla js로 만든 목업이라 Next.js의
서버/클라이언트 컴포넌트 분리, 데이터 페칭 방식, 상태 관리 방식과는
무관하다. 구조는 새로 설계하고, **내용(카피·규칙·토큰)만** 가져올 것.

각 파일 안에 실제 관측 데이터처럼 보이는 숫자(예: "38/126건", "504개
응답")가 있는데, 이건 전부 화면 설계용 예시 데이터다. 실제 Supabase 값과
헷갈리지 말 것.

## 이 폴더에 포함된 참고 문서

- `docs/ensl-aeo-mvp-prd.md` — 제품 정의 PRD v0.2
- `docs/day4-decision-schedule.md` — 측정 스케줄 결정 기록
- `docs/claude_day8-decision-citation-linking.md` — 출처 연결 판정 규칙
- `docs/claude_day9-decision-aggregation.md` — 집계 로직(aggregated_metrics)
  결정 기록
- `docs/prototypes/*.html` — 화면 설계 목업 14개 (위 설명 참고)

작업 중 이 문서들과 충돌하는 내용을 발견하면, 임의로 해석하지 말고
루아에게 먼저 확인할 것.
